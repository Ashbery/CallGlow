# 手錶端專案（本機 JDK 21）
# v1.0.3：Pulsar 帶入 androidx 依賴 → 啟用 AndroidX（D6 部分鬆綁，見 decisions D21）
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# 關閉 AGP 非 ASCII 路徑檢查（相容於任何含中文/空白的工作區路徑）
android.overridePathCheck=true
